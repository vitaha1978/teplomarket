<nav class="navbar navbar-default welly" id="navbar" role="navigation">
	<div class="container">

		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/">
				<%= image_tag("logo.png", alt: "ТеплоМаркет") %>
			</a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="navbar-collapse">
			<ul class="nav navbar-nav">
        <li <%= controller.controller_name == 'products' ? raw(' class="active"') : '' %>>
          <%= link_to "Інтернет магазин", products_path %>
				</li>
				<li <%= controller.action_name == 'contacts' ? raw(' class="active"') : '' %> >
					<%= link_to "Контакти", contacts_path %>
				</li>
        <li <%= controller.controller_name == 'articles' ? raw(' class="active"') : '' %> >
					<%= link_to "Статті сайту", articles_path %>
				</li>
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">Розділи сайту <b class="caret"></b></a>
					<ul class="dropdown-menu">
			       <li class="dropdown-header">
							Електричне опалення
						</li>
						<li>
							<%= render(partial: "categories/category", collection: @categories) %>
						</li>			
          </ul>
				</li>
			</ul><!-- ul > navbar-nav -->
			<form class="navbar-form navbar-left" role="search">
				<div class="form-group">
					<input type="text" class="form-control" placeholder="Пошук по сайту...">
				</div>
				<button type="submit" class="btn btn-default"><%= image_tag("search-icon.png", alt: "Пошук по сайту", class: "search") %></button>
			</form><!-- form navbar-nav -->
			<ul class="nav navbar-nav navbar-right">				
				<% if signed_in? %>
          <li><%= link_to "Користувачі", users_path %></li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Мій профіль <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Профіль", current_user %></li>
                <li><%= link_to "Налаштування", edit_user_path(current_user) %></li>
                
                <li class="divider"></li>
                <li>
                  <%= link_to "Вийти", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Увійти", signin_path %></li>
          <% end %>
			</ul>
		</div><!-- navbar-collapse -->
	</div><!-- container -->
</nav><!-- End navbar -->